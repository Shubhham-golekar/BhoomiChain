{
    "version": 2,
    "buildCommand": "cd frontend && npx vite build --mode preview",
    "outputDirectory": "frontend/dist",
    "installCommand": "npm install",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}